# SiteFetcher
Local item price and stock scanning and notification for Canada Retailer

## Project structure
- `cmd/sitefetcher/main.go` — application entrypoint
- `internal/fetcher` — fetching and request logic
- `internal/stores` — store factory and store-specific implementations

## Run
Use:

```bash
go run ./cmd/sitefetcher
```
